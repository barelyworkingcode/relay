# Scenario: permission-prompt-active

Overlay that simulates a pending permission prompt from a running
session — used by the permission-flow screencast.

Overlay strategy: drops a marker file the Service Inspector renders as
a "pending approval" badge on the field-notes project row.
