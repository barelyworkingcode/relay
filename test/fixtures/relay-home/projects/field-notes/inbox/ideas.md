# Inbox

Unsorted thoughts. Triage weekly.

- Try the new clustering algorithm on the Q4 dataset — see if it
  separates the two cohorts more cleanly than k-means did.
- Ask the IT team whether we can get a small GPU on the cluster for
  pipeline prototyping.
- A new paper landed last week on batch normalization for microscopy —
  add to the reading list and decide if it changes the proposal in
  `2026-Q1/research.md`.
